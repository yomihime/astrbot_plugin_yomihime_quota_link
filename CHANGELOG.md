# 更新记录

## 未发布（当前元数据版本 v0.1.0）

### 新增

- 建立 AstrBot 插件骨架、额度领域模型、配置校验和查询服务。
- 支持命令与自然语言查询、缓存、并发限制和账户级错误隔离。
- 增加 DeepSeek 余额、阿里云账户现金余额和可配置只读余额端点三类适配器。
- 增加统一 HTTP 传输、配置界面字段和依赖声明。
- 增加本地插件 ZIP 构建脚本，并在推送新的 `v*` tag 时由 GitHub Actions 检查、测试和构建，上传工作流 artifact。
- 增加 Grsai 独立供应商的账户积分查询，可选择国内直连或海外节点；余额查询令牌仅用于官方账户积分接口的请求体。
- 增加只读账户能力列表，让模型查询可用账户时无需逐个访问供应商。

### 变更

- LLM 工具返回结构化查询事实，由模型组织最终回复；`/yql` 命令仍直接返回插件文本。
- 各供应商提供独立配置模板，Grsai 新条目不再填写 Host、请求路径或响应映射；缩短添加菜单与字段标签，将详细说明放在展开表单内。
- DeepSeek 和阿里云百炼增加常用名称匹配；阿里云百炼模板明确查询阿里云账户现金余额及所需的财务只读 RAM 权限。

### 修复

- 修复旧兼容服务完整 URL 覆盖新 Host/API Path，以及旧 Grsai 配置迁移时可能错误切换地区的问题。
- Grsai 账户积分与 API Key 积分范围分别处理；未实现的 API Key 积分范围不会误报为可查询。

### 说明

- 用户已在 AstrBot ChatUI 实测 Grsai 账户积分查询和自然语言工具回复；截图显示成功结果及缓存标记。海外节点、Grsai 错误响应语义仍未经过真实账户验证。
- 阿里云现金余额需要具有财务只读权限的 RAM AccessKey。本仓库尚未创建版本 tag，本节仍为未发布记录。
